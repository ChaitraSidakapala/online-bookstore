apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: bookstore
type: Opaque
stringData:
  # PostgreSQL root credentials
  postgres-password: "bookstore_admin_pass_2025"
  postgres-user: "postgres"
  
  # Application database credentials
  database-name: "bookstore"
  database-user: "bookstore"
  database-password: "bookstore123"
  
  # Connection strings for services
  catalog-database-url: "postgresql://bookstore:bookstore123@postgres-service:5432/bookstore"
  order-database-url: "postgresql://bookstore:bookstore123@postgres-service:5432/bookstore"
